schema_version: 1.0
decision: pending_re_evaluation
source_report: null
production_default: multi_agent_for_eligible_tasks
multi_agent_enabled_by_default: true
roles:
  reader_parallel: default_for_eligible_tasks
  evidence: default_for_eligible_tasks
  critic: default_for_eligible_tasks
  verifier: default_for_eligible_tasks_with_deterministic_boundary
bounded_revision_enabled: true
runtime_gates:
  MULTI_AGENT_ENABLED: true
  ALLOW_EXPERIMENTAL_NO_GO: true
experimental_full_revision_max_rounds: 1
reason: >-
  Product policy enables the bounded multi-Agent DAG for eligible multi-paper tasks.
  Previous result artifacts came from an older code path and are not evidence for
  the current runtime; a fresh evaluation is required.
