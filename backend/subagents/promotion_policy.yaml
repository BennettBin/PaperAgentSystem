schema_version: 1.0
decision: no_go
source_report: evaluation/reports/n05_multi_agent_production_v2/report.json
production_default: single_agent
multi_agent_enabled_by_default: false
roles:
  reader_parallel: experimental_only
  evidence: experimental_only
  critic: experimental_only
  verifier: experimental_only_with_deterministic_boundary
full_revision_enabled: false
experimental_override_requires:
  MULTI_AGENT_ENABLED: true
  ALLOW_EXPERIMENTAL_NO_GO: true
experimental_full_revision_max_rounds: 1
reason: >-
  The 90-case offline-real-model ablation missed the Claim Support and Token gates;
  the extra Verifier LLM pass had zero quality gain and full revision reduced Claim Support.
