# PDF 布局与溯源规则

- 每页先判断单栏或双栏；双栏阅读顺序固定为左栏自上而下，再右栏自上而下。
- 正文块保留 `page`、`bbox`、`section_path` 和块序号。
- 图、表、算法区域独立裁剪为 PNG，正文 Chunk 只保留标题或说明，不混入区域主体文本。
- 视觉材料记录类型、标签、页码、章节、bbox、caption、artifact ID 和对象存储路径。
- 扫描件或低质量页面必须标记解析质量，不把 OCR 猜测当作确定文本。
